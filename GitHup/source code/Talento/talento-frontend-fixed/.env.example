# Talento Frontend – Environment Variables
# Copy this file to .env.local for local development (never commit .env.local)

# Backend URL (dev: direct to Spring Boot; Docker: leave empty for nginx /api proxy)
VITE_API_URL=http://localhost:8080
